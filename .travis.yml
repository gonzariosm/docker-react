language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t xalupeao/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true xalupeao/docker-react npm run test